{{- if .Values.apps.threadlyBackend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apps.threadlyBackend.name }}
  namespace: {{ .Values.apps.threadlyBackend.namespace }}
  labels:
    app: {{ .Values.apps.threadlyBackend.name }}
spec:
  type: {{ .Values.apps.threadlyBackend.service.type }}
  ports:
  - port: {{ .Values.apps.threadlyBackend.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.apps.threadlyBackend.name }}
{{- end }}
